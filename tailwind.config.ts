import type { Config } from "tailwindcss";

const config: Config = {
  // In Tailwind v4, colors defined in @theme are automatically available
  // No need to define them here if they're in globals.css @theme block
};

export default config;
